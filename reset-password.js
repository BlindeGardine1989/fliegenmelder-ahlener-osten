import{createClient}from"https://cdn.jsdelivr.net/npm/@supabase/supabase-js/+esm";import{SUPABASE_URL,SUPABASE_ANON_KEY}from"./config.js";const supabase=createClient(SUPABASE_URL,SUPABASE_ANON_KEY),input=document.querySelector("#newPassword"),button=document.querySelector("#savePassword"),status=document.querySelector("#resetStatus");button?.addEventListener("click",async()=>{const e=input.value.trim();if(e.length<8)return void(status.textContent="Das Passwort muss mindestens 8 Zeichen lang sein.");status.textContent="Passwort wird gespeichert ...";const{error:t}=await supabase.auth.updateUser({password:e});t?(console.error(t),status.textContent="Passwort konnte nicht gespeichert werden."):status.textContent="Passwort wurde gespeichert. Du kannst dich jetzt einloggen."});